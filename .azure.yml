trigger:
  branches:
    include:
    - master

  tags:
    include:
    - '*'

jobs:
  #### Clang ####

  - template: build/ci/linux.yml
    parameters:
      toolchain: 'clang'
      mode: 'Debug'
      arch: 'x86'

  - template: build/ci/linux.yml
    parameters:
      toolchain: 'clang'
      mode: 'Debug'
      arch: 'x64'

  - template: build/ci/linux.yml
    parameters:
      toolchain: 'clang'
      mode: 'Release'
      arch: 'x86'

  - template: build/ci/linux.yml
    parameters:
      toolchain: 'clang'
      mode: 'Release'
      arch: 'x64'

  #### GCC ####

  - template: build/ci/linux.yml
    parameters:
      toolchain: 'gcc'
      mode: 'Debug'
      arch: 'x86'

  - template: build/ci/linux.yml
    parameters:
      toolchain: 'gcc'
      mode: 'Debug'
      arch: 'x64'

  - template: build/ci/linux.yml
    parameters:
      toolchain: 'gcc'
      mode: 'Release'
      arch: 'x86'

  - template: build/ci/linux.yml
    parameters:
      toolchain: 'gcc'
      mode: 'Release'
      arch: 'x64'

  #### Windows ####

  - template: build/ci/windows.yml
    parameters:
      mode: 'Debug'
      arch: 'x86'

  - template: build/ci/windows.yml
    parameters:
      mode: 'Debug'
      arch: 'x64'

  - template: build/ci/windows.yml
    parameters:
      mode: 'Release'
      arch: 'x86'

  - template: build/ci/windows.yml
    parameters:
      mode: 'Release'
      arch: 'x64'
